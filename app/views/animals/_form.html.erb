<%= form_for(@animal) do |f| %>
  <% if @animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% @animal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :habitat %><br />
    <%= f.text_field :habitat %>
  </div>
  <div class="field">
    <%= f.label :nocturnal %><br />
    <%= f.check_box :nocturnal %>
  </div>
  <div class="field">
    <%= f.label :diet %><br />
    <%= f.text_field :diet %>
  </div>
  <div class="field">
    <%= f.label :weight %><br />
    <%= f.number_field :weight %>
  </div>
	<br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
